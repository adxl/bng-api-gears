version: '3'

services:
  api:
    image: adxl/bng-api-gears:latest
    build:
      context: .
      target: production

  db:
    image: adxl/bng-db-gears:latest
    build:
      context: .
      dockerfile: Dockerfile.db
