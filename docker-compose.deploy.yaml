version: '3'

services:
  gears-api-service:
    image: adxl/bng-api-gears:latest
    environment:
      STAGE: production
      DATABASE_URL: '@DATABASE_URL@'
    networks:
      - bng
    deploy:
      replicas: 3

networks:
  bng:
    external: true
